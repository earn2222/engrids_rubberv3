services:
  postgis:
    platform: linux/amd64
    build: ./postgis
    container_name: rub_postgis_v3
    restart: always
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=postgres
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - pgdata_v3:/var/lib/postgresql/data
    networks:
      - rub-network-v3
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d postgres" ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s

  nodejs:
    build: ./nodejs
    container_name: rub_nodejs_v3
    restart: always
    working_dir: /usr/src/app
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_HOST=postgis
      - DB_PORT=5432
    depends_on:
      postgis:
        condition: service_healthy
    volumes:
      - uploads_data_v3:/usr/src/app/uploads
    ports:
      - "4400:3000"
    networks:
      - rub-network-v3
    command: sh -c "nodemon server.js"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:3000/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

  backup:
    build: ./backup
    container_name: rub_backup_v3
    restart: always
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=postgis
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
      - BACKUP_INTERVAL_HOURS=24
      - BACKUP_KEEP_DAYS=7
      - BACKUP_DIR=/backups
    depends_on:
      nodejs:
        condition: service_healthy
    volumes:
      - backups_data_v3:/backups
      - ./backups:/backup-host
    ports:
      - "9100:8100"
    networks:
      - rub-network-v3

networks:
  rub-network-v3:
    driver: bridge

volumes:
  pgdata_v3:
  uploads_data_v3:
  backups_data_v3:
